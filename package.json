{
  "name": "gowingo-demo",
  "version": "1.0.0",
  "private": true,
  "description": "GoWingo delivery demo app (web + Capacitor Android wrapper)",
  "scripts": {
    "serve": "npx --yes serve www",
    "apk:setup": "npm install && npx cap add android && npx cap sync android",
    "apk:build": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  }
}
